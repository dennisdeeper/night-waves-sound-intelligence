# Night Waves R&D v0.7 — Host Discovery + Isolated Mount

Repository audit result:
- `sampling-workspace.html` is not production Sampling source. Its first lines identify it as a saved ChatGPT conversation page.
- `app.js` is a real Web Audio implementation with microphone input, monitor state, world DSP chains and output routing. It is therefore treated as protected audio code and is not used as the Find Sound host.
- GitHub code search did not identify a reliable Track Intelligence host marker, so v0.7 does not guess or patch a host file.

Built instead:
- `nw-host-contract.js`: narrow event-only recommendation handoff.
- `nw-find-sound-mount.js`: isolated Find Sound mount.
- `nw-find-sound-mount.css`: theme-aware Warm Studio / Cyan-Red styling.
- `demo-v0.7-mount.html`: host simulator proving the contract without production audio systems.

Promotion rule:
Do not mount into production until the genuine Track Intelligence host is positively identified and regression/human checks are complete.
